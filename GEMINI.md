# Gemini Project Analysis

This document provides a high-level overview of the Trade_Stock project, generated by Gemini.

## Project Structure

The project is a Django application with the following key directories:

- **`analysis`**: Contains the core business logic for fetching, processing, and storing stock data. This includes models, views, and data update scripts.
- **`api`**: Likely contains REST API endpoints, though the current structure seems more focused on Django's MTV pattern.
- **`SyncApp`**: Appears to be a Django app for database synchronization tasks.
- **`templates`**: Contains the HTML templates for rendering the user interface.
- **`trading_analysis`**: The main Django project directory, containing settings and project-level URL configurations.
- **`data`**: A directory for storing data files, such as JSON files with symbols or other relevant information.
- **`static`**: (Not present, but typical) Would contain static assets like CSS, JavaScript, and images.
- **`stock_venv_01`**: A Python virtual environment for managing project dependencies.

## Key Components

- **`analysis/models.py`**: Defines the database schema, including models for storing stock prices, news, and earnings data.
- **`analysis/views.py`**: Contains the view functions that handle user requests and render the appropriate templates.
- **`analysis/stock_earning_update.py`**: Contains the logic for fetching and updating earnings data from `yfinance`.
- **`trading_analysis/urls.py`**: Defines the URL patterns for the entire project, mapping URLs to their corresponding views.
- **`templates/`**: Contains various HTML files for different pages of the application, such as the home page, charts, and data update forms.
- **`requirements.txt`**: Lists the Python dependencies for the project, which are managed using the `stock_venv_01` virtual environment.

## Dependencies

The project relies on several key Python libraries, including:

- **`Django`**: The web framework used to build the application.
- **`yfinance`**: A library for fetching financial data from Yahoo Finance.
- **`pandas`**: Used for data manipulation and analysis.
- **`plotly`**: A library for creating interactive charts.
- **`psycopg2-binary`**: A PostgreSQL adapter for Python.

## Core Functionality

The project appears to be a stock analysis and trading application with the following features:

- **Data Fetching**: Fetches stock data, news, and earnings information from `yfinance`.
- **Data Visualization**: Displays stock data in interactive charts using Plotly.
- **Watchlists**: Allows users to create and manage watchlists of stocks.
- **Earnings Calendar**: Displays upcoming and past earnings dates for stocks.
- **Admin Interface**: Provides a Django admin interface for managing the database.
